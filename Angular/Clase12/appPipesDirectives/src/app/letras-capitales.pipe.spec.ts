import { LetrasCapitalesPipe } from './letras-capitales.pipe';

describe('LetrasCapitalesPipe', () => {
  it('create an instance', () => {
    const pipe = new LetrasCapitalesPipe();
    expect(pipe).toBeTruthy();
  });
});

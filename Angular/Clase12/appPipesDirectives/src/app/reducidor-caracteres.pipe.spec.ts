import { ReducidorCaracteresPipe } from './reducidor-caracteres.pipe';

describe('ReducidorCaracteresPipe', () => {
  it('create an instance', () => {
    const pipe = new ReducidorCaracteresPipe();
    expect(pipe).toBeTruthy();
  });
});
